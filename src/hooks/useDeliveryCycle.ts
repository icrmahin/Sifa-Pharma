import type { DeliveryCycle } from "../types/deliveryCycle";

// Placeholder: backend not yet implemented.
export function useDeliveryCycle() {
  const cycle: DeliveryCycle | null = null;
  const loading = false;
  const reload = async () => {};
  return { cycle, loading, reload };
}
